# Bank-Statement-Analyzer
Automates the consolidation of bank statements from multiple institutions. Cleans, standardizes, and merges CSV files into a unified dataset, classifies transactions, and generates a formatted Excel report using Python, Pandas and OpenPyXL.
